# Embed errors

This directory owns the crate-wide error and `Result` alias for deterministic
validation at the host-neutral embedding boundary.

| File | Purpose |
| --- | --- |
| `mod.rs` | Public error variants and the crate-wide result alias. |
