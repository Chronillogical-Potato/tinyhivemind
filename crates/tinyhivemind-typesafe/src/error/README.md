# TypeSafe adapter errors

This directory owns the adapter's crate-wide typed errors and result alias.

| File | Purpose |
| --- | --- |
| `mod.rs` | Native validation, conversion, transport, and state-encoding errors. |
