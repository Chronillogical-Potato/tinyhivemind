# Decision evaluation module

`mod.rs` drives paired Jev and strict-JSON calls and aggregates measurements.
`schema.rs` builds the exact dynamic JSON Schema for the LLM baseline, while
`test.rs` covers the fixed corpus, schema, metrics, and curl-config escaping.
