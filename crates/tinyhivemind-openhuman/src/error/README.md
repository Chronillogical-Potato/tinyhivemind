# `error`

The crate error. A refused runner name, an incomplete route, a seat the
registry did not load, a turn past its wall, the tool server failing to
bind, a definition failing to write, an agent failing to instantiate, and
`Harness` for what OpenHuman itself refuses.
