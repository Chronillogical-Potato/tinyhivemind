# DeepSWE disposable fixture

Tests copy these files into a temporary directory and initialize a new local
Git repository. `answer.txt` starts wrong, and `test.sh` passes only after the
expected one-line edit. No downloaded dataset or network service is involved.
